#include "Node.h"

//Node constructor
Node::Node(int userData)
{
    data = userData; //Inserting userData 
    next = NULL; //Inserting NULL in next
    prev = NULL; //Inserting NULL in prev
}
